# Integrated beta status

Latest daytime web UI integration: [web-ui-integration.md](web-ui-integration.md)
records the completed remaining-screen source pass plus a profile-save race fix.
Current local web build is **`RHF3PnWQrDsVQJjkCCXDQ`**, with 48 focused checks,
web TypeScript and production build passing. The 337-source/476-output fingerprint
is new; prior identities below are historical. Security, billing, native source
and all three APKs are preserved. No new browser, device or owner visual acceptance.

31 August image-security follow-up: the unused Next optimizer is disabled and its
wildcard remote host allowance removed. That checkpoint's local web build was
`JU_bSJRJvJgnvh6kH4QVX`; the bundled parser is unchanged and remains a trusted-build
risk. [Exact exposure, tests and preservation evidence](image-parser-security.md)
supersede only the web configuration/build identity below. All three APKs and
tested billing source remain unchanged; no browser/device/deployment acceptance.

30 August 2026. Canonical source: `C:\Users\James\Documents\GitHub\SecondBreakfast`, branch `codex/whimsical-private-beta`. This is a local review candidate, not a cohort release. The four parallel tasks and their exact ownership remain recorded in `COORDINATION.md`.

## Concept rebuild integrated; visual acceptance pending

31 August overnight follow-up: [overnight-integration.md](overnight-integration.md)
records secure Stripe lifecycle hardening, Android concept parity source, 551 core
tests, all four typechecks, 33 actual-route/PGlite billing scenarios and six real
PostgreSQL scenarios. Both new account/guest Android review artifacts and the fresh
production web build passed. That prior web build ID was `8SppsANVHSszqZHJ20R5P`.
The older build ID and unchanged-Android statement below describe the prior
checkpoint. Neither new visual nor device acceptance is claimed. The cloud
coordinator has selected a new development account with no app runtime yet; do
not reuse the prior account as an implicit deployment target.

James rejected the palette-only treatment and explicitly requested an interface matching `design/concepts/woodland-beta-board.png`. The new web source implements the shelf sidebar and recipe-card grid, illustrated kitchen composition, brass-framed timber surfaces, parchment sections, labeled icon navigation and image-led care choices. Its [fresh integration evidence](web-concept-integration.md) records the successful production build, all four typechecks, 551 core tests, 13 care helpers, 13 authorization/library/report tests and ten offline checks. Integration restored the care ingredient list and kept the new library art exclusive to the server-selected beta view.

Current web build ID: `xRryI_oXqVxfyyv5CUkPT`. Lossless artwork derivatives reduce the four files by 27.9% with identical decoded pixels; original PNGs remain. The web build, source fingerprint and screenshots in the older table below are **prior-revision evidence**. Logs/fingerprints are preserved; the old compiled output has been replaced. The Android APK remains unchanged and does not match the new web visual revision. Browser URL-policy restrictions remain in force, so this rebuild still has no running-UI screenshots or visual acceptance.

## Working implementation

The kitchen visual system uses separate raster scenes/props/icons and native controls. Care has a shared curated ranker, strict choices, detected-allergen exclusion with explicit matching limits, guest native access, temporary local settings, honest shopping feedback and a restricted handoff protocol. Hosted care uses the server Clerk allowlist. The revised worker stores public resources and a synthetic generic offline document; no public online HTML page bypasses care authorization.

Recipe/index and billing changes now use real database transactions. Effective-host TLS parsing cannot use a localhost authority to conceal a remote query override. The Stripe source handoff is integrated, including disabled paid controls and atomic, deduplicated fulfillment. Checkout and live mode remain off. No live integrations, cloud resources or tester invitations were created.

## Prior integrated snapshot checks

| Check | Result | Evidence |
| --- | --- | --- |
| Core suite after Stripe and offline integration | 551 passing tests | `checks/integrated-core-tests.txt` |
| Core, database, web and mobile typechecks | All four passed again after final source changes | `checks/integrated-typecheck-summary.txt` and per-workspace logs |
| Web/mobile care, save and link helpers | 13 passed, including the bounded ambiguous-write case | `checks/integrated-care-ui-tests.txt` |
| Final production web build | Passed, including type validation and 35 prerendered pages; `/care` remains dynamic | `checks/integrated-web-build.txt` |
| Regenerated worker/privacy parity after the final web build | 10 tests passed | `checks/integrated-offline-final.txt` |
| Android arm64 release build | Passed; final incremental build includes care access during account loading | `checks/android-final-review-build.txt`, `checks/android-artifact-manifest.json` |
| Android artifact/source identity | APK hash verified; all 107 source and three dependency hashes match | `checks/android-root-manifest-verification.json` and native manifests |
| Actual web gate/page and image-report code with mocked boundaries | 9 passed | `checks/integrated-authorization-image-tests.txt` |
| Actual release script with mocked AWS/Docker boundaries | 56 assertions passed | `checks/integrated-release-tests.txt` |
| Existing disposable database suites | 335 assertions in 15 suites | `care-data-evidence.md` and `checks/care-data-summary.json` |
| Recipe/index rollback and account isolation | 10 scenarios passed | `checks/care-data-transactions.txt` |
| Billing rollback and actual multi-connection PostgreSQL locking | 5 scenarios passed; old-code regression reproduced first | `stripe-integration-evidence.md` |
| Isolated and integrated PGlite billing test | Passed | `stripe-integration-evidence.md` |
| Worker v3 browser install/update/offline/strict no-match | Passed with synthetic identities and failed-network simulation | `web-evidence.md`, `readiness-evidence.md` |

Counts apply to the named runs. They do not imply authenticated Clerk, production RDS, physical airplane mode, OS app dispatch, accessibility certification or device validation. The production web artifact is in `apps/web/.next-build`. The web build used blank Clerk/database/paid-service credentials, beta disabled and worker compilation enabled. It is a compilation check, not an account-enabled cohort image or deployment.

The final [Android review APK](../../apps/mobile/.expo-export/account-preview/second-breakfast-arm64-review.apk) is 51,258,957 bytes, package `com.secondbreakfast.app`, version `0.1.0` (1), arm64-v8a. SHA-256: `b828dcf86cbba709d0fc1905a38e5cf1495cb8acd789e4c2357aae0fe4d82957`. APK v2 signature verification passes with `CN=Android Debug`, a publicly known development signing identity. This is an account-preview build with a test Clerk key and bundled guest care; it is not approved for cohort distribution. Packaged markers include the final account-loading care link, pantry limitation, account-pinned tokens and uncertain-write timeout. The earlier `a20eb8...` APK predates that final care link and is superseded.

After the web lane's handoff, integration also applied the existing bounded-request wrapper to care shopping writes. A response that never arrives now becomes unconfirmed after 12 seconds instead of leaving the button busy indefinitely. The request is not automatically retried or called unsaved; it might have committed. The web typecheck passed again. Browser validation of that case remains part of the blocked real-session checks.

The local web [source/artifact fingerprint](checks/web-review-manifest.json) records build ID `ro4vTMQRf_hwjKRyZWS13`, 315 source/configuration files and 475 generated artifact files. It excludes build cache/trace and reads no environment values. This binds the local review snapshot; it is not signed container provenance or a reviewed source commit. The independent restaurant-discovery proposal is preserved in its separate `codex/restaurant-discovery` worktree. Studio legal changes remain preserved in the shared working tree. No combined commit or push has been made.

## Review material

- [Concept board](../../design/concepts/woodland-beta-board.png) and [optional Wispling visitor comparison](../../design/concepts/wispling-visitor-comparison.png). The beta has no character by default.
- [Wide kitchen library](web-library-desktop-dark.jpg), [care at 200 percent text](web-care-narrow-light-200.jpg), and [cook at 200 percent text](web-cook-narrow-dark-200.jpg). See the web report for which changes came after each capture.
- [Web evidence](web-evidence.md), [Android evidence](android-evidence.md), [care/data evidence](care-data-evidence.md), [privacy/release evidence](readiness-evidence.md), and [Stripe review](stripe-integration-evidence.md).
- [Tester guide](tester-guide.md) and [enrollment, removal, release and rollback runbook](release-runbook.md).

Browser screenshot file signatures were checked. The six earlier `care-*`/`cook-runtime` captures also contained JPEG bytes, so their extensions were corrected to `.jpg` without changing the images. The concept and production PNG artwork is separate.

## Unfinished release gates

1. The APK compiles and its development signature verifies, but owner-controlled release signing and device checks remain. Exact Expo-compatible Worklets/Reanimated pins fixed a demonstrated dependency mismatch; the successful build uses the documented Windows path workaround. No emulator or physical-device validation is claimed. The fresh adb device list is empty.
2. Real Clerk sign-in/invitation, expiration, account switching and authenticated failed-write checks remain. Browser URL policy explicitly denied the last app reload; no retry or alternate-access workaround was attempted. Remaining post-fix forms/plans screenshots and accessibility checks await permitted browser access.
3. Wispling's isolated handoff targets the legacy baseline. The active alpha entry selects `src/alpha/AlphaApp`, where those legacy routes are unreachable, and that alpha milestone explicitly excludes this integration. The tested legacy patch and an active-alpha adaptation proposal are available, but no alpha changes or complete current-alpha handoff are claimed. See `handoff-evidence.md`. OS-level two-app tests also remain.
4. AWS authentication remains expired on a fresh identity check. Current deployed revision, scale-down state, reviewed image provenance and a privacy-hardened rollback image are unverified. A privacy-only bridge release may be needed so automatic rollback cannot restore the old cache defect.
5. Final source review, approved signing, usability/safety review, a private manual feedback destination and consenting testers' Clerk IDs are required before rollout. A later read-only audit verified the Stripe test account, exact app-scoped catalog, and compatible portal configurations; no webhook endpoint exists and checkout stays off. Public release is outside this pass.

No permission is needed to continue ordinary local implementation. The release gates above require actual evidence or external access; they are not satisfied by compilation or mock tests.
